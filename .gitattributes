# Keep shell scripts, the Python patcher, and Linux launchers LF on every
# platform (CRLF breaks bash and .desktop launchers).
*.sh      text eol=lf
*.py      text eol=lf
*.desktop text eol=lf
# Windows launchers stay CRLF.
*.bat   text eol=crlf
